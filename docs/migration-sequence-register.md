# LAWCASEPRO V5 — MIGRATION SEQUENCE REGISTER

> ## ⚠️ OUT-OF-BAND (OOB) MIGRATION CHANNEL — FROZEN (EFFECTIVE 2026-08-11)
>
> The secondary migration channel under `supabase/migrations/*.sql` (used for emergency P0 runtime patches, prefixed `vsr_*` or `reconcile_*`) is **PERMANENTLY FROZEN** as of this corrective register update.
>
> **RULES:**
> 1. **NO NEW OOB SCHEMA MIGRATIONS SHALL BE CREATED.** All future schema changes must use the **ONE canonical numeric chain**: `lib/db/migrations/NNNN_description.sql` (strict ascending, no gaps invented, no renumbering of already-applied migrations).
> 2. Existing OOB files that have been **REMOTE APPLIED** (`vsr_p2_doc_gen_logging_additive.sql`, `vsr_addendum_p0_runtime_schema_parity.sql`, `reconcile_pv_idempotency_and_actions.sql`) are **retained in place for reconciliation/audit** and MUST NOT be deleted or renamed. They remain **NON-CANONICAL** — their object contracts have been folded into canonical numeric migrations where applicable.
> 3. Duplicate/mirror OOB migrations (`vsr_p3_*`) that simply mirrored canonical 0148/0149/0150 **have been DELETED** from the OOB directory (they were never applied remotely; they existed only as local working copies created in violation of the OOB freeze). See §2 entries marked `OOB-DUPLICATE-DELETED`.
> 4. OOB channel is read-only/audit-only. It is not an active migration mechanism.
> 5. Any future emergency hotfix requiring immediate DDL without passing through the numeric runner must be recorded as an explicit one-off entry in this register with a timestamp, operator signature, and supersedes canonical migration id; the hotfix SQL itself may be placed in `supabase/migrations/` as a ONE-OFF exception with a clear header block stating it, and an accompanying canonical migration MUST be issued as soon as possible (same sprint) to fold the change back into the numeric chain so history catches up.

Date of Audit: 2026-08-11 (Tier 1 Corrective P0 — Source-of-Truth Recovery & Migration Reconciliation)
Auditor: FullStack Engineer (P0 Corrective — 4-way Reconciliation: Local / Git-hotfix / GitHub / Supabase Production)
Goal:
1. Verify Local HEAD / Remote HEAD / Working tree / Production schema are reconcilable.
2. Fill register 0147–0151 with actual reality (proxy-verified via supabase_get_tables), not assumptions.
3. Explicitly verify 0147 applied, 0151 applied, resolve 0150 meaning, detect SCHEMA_DRIFT.
4. Retire/Delete duplicate vsr_p3 mirror OOB files; mark OOB channel FROZEN per P0 mandate.

---

## 1. Evidence Sources / Method

| Source | Evidence |
|---|---|
| `LS lib/db/migrations` | Local migration list 0000…0143 confirmed. 0142 = unique constraints, 0143 = firm_number_sequences new. |
| `scripts/src/remote-migration-and-duplicate-preflight.mjs` | Node ESM READ-ONLY audit script written. **Executed BUT BLOCKED**: Supabase pooler returned `(ENOTFOUND) tenant/user postgres.bepixycuulklorcbadww not found` for all aws-0 / aws-1 pooler endpoints; direct host `bepixycuulklorcbadww.supabase.co:5432` timed out on TCP (port likely firewall-blocked). |
| `supabase_get_tables(public, [permissions, invoices, receipts, payment_vouchers])` | Indirect proxy for remote column count → strong evidence for 0122 applied vs 0136/0140 NOT applied. |
| `LS lib/db/src/schema/*.ts` + `docs/` | Local schema + Final Completion Register + Historical migration sequence register. |

### 1a. `scripts/src/remote-migration-and-duplicate-preflight.mjs` — Blocker Details

Connection attempts (all pooler URLs correctly pass SSL SNI/options):

| # | Config | Result |
|---|---|---|
| 1 | `postgres.PROJECT_REF` + pw service_role_key @ `aws-0-ap-southeast-1.pooler.supabase.com:5432` SSL | `(ENOTFOUND) tenant/user postgres.bepixycuulklorcbadww not found` |
| 2 | `PROJECT_REF` (raw) + pw @ `aws-0 pooler:5432` SSL | Same tenant not found. |
| 3 | `postgres` + pw + `options=external_id=PROJECT_REF` @ `aws-0 pooler:5432` SSL | `(ENOIDENTIFIER) no tenant identifier provided (external_id or sni_hostname required)` |
| 4 | URL-string encoded `postgres.REF:pw@aws-0 pooler:5432` options=project=REF | Tenant not found. |
| 5 | URL `postgres:pw@bepixycuulklorcbadww.supabase.co:5432/postgres sslmode=require` | **Connection terminated due to connection timeout** (TCP 5432 not reachable from runner egress). |
| 6 | `audit-and-apply-0122.mjs` original aws-1 pattern | User stripped to `postgres` + password auth failed. |

**Conclusion re: direct SQL access** to remote `schema_migrations`: **Currently BLOCKED via pg.Pool** for this specific Supabase project identifier scheme. Pooler host resolves but tenant identity format mismatch (may require Supabase DB password explicitly — not JWT service_role key — for this project, or project may use a newer naming scheme not yet supported by the aws-0/aws-1 pooler).

Proxy via `supabase_get_tables` used below for column-level conclusions; row-level duplicate check and exact `schema_migrations.version` list remain unverified from the direct-SQL angle (see `docs/accounting-duplicate-preflight.md` for explicit "UNABLE TO VERIFY VIA DIRECT SQL" label).

---

## 2. Register 0120 – 0143 (Full Columns V2 §7)

Legend for **Remote History Exists (Y / N / UNVERIFIED-LOCAL-ONLY)**:
- **Y** = Concretely verified (either via direct SQL row or via column count proxy that leaves no other plausible explanation).
- **N** = Concretely verified ABSENT (required additional columns NOT present via supabase_get_tables; implies migration not run).
- **UNVERIFIED-LOCAL-ONLY** = Local file exists; neither direct SQL nor schema proxy gave a concrete verdict; default assume NOT applied.

| # | Filename | Module | Remote History Exists | Local File Exists | Destructive DDL? | Depends On | Can Apply Out-of-Order (bypassing HRMS 0127–0135 numeric span)? | Blocked By / Notes | Proposed Final Number / Channel (CORRECTIVE G4 strict numeric) |
|---|---|---|---|---|---|---|---|---|---|
| 0120 | `0120_case_notifications_and_reference_proposed.sql` | Case Notifications baseline | **Y** (Proxy: notifications-related case tables present; merged pre-0122; historical lock) | Y | No (additive) | 0119 & earlier | N (locked history) | None; locked history — never re-run. | 0120 (locked) |
| 0121 | `0121_email_compose_phase.sql` | Email (FROZEN per task guard) | **UNVERIFIED-LOCAL-ONLY** → assume N remotely; module frozen anyway | Y | No (additive; Email tables only) | 0120 | N (frozen module — DO NOT TOUCH per user instruction: "DO NOT touch Email…") | **EMAIL MODULE EXPLICITLY FROZEN by user; skip.** | 0121 (locked, no edits) |
| 0122 | `0122_accounting_settings_and_payment_voucher_sla.sql` | Accounting: `payment_vouchers` (57 cols incl. `received_at…late_completion_reason`) + `payment_voucher_actions` + `accounting_settings` + `user_notifications` baseline (13 cols) | **Y** — PROXY-VERIFIED via `supabase_get_tables` 2026-08-07: `payment_vouchers` = exactly 57 columns (contains 0122-only PV SLA cols `received_by…late_completion_reason`); `user_notifications` referenced FK by remote implies table baseline exists. | Y | No (pure ALTER ADD IF NOT EXISTS + CREATE TABLE IF NOT EXISTS) | 0121 | N (locked history) | None; protected history — never re-run. Remote apply = YES already. CORRECTIVE G7 Static PASS but never re-apply. | 0122 (locked; never rewrite file) |
| 0123 | `0123_payment_voucher_idempotency_and_perf.sql` | PV Idempotency Indexes + `uq_payment_vouchers_client_request` | **UNVERIFIED-LOCAL-ONLY** → assume N | Y | No (CREATE INDEX IF NOT EXISTS) | 0122 | **N (CORRECTIVE G4 CANCELLED out-of-order proposal)** | Depends on 0122 baseline Y. Strict numeric ascending only. Would apply after 0122, then runner skips 0124/0125 (gaps, never invent), then 0126, then 0127-0135 HR block (blocked at G10 2/6 gate → HOLD). CORRECTIVE G6 HOLD: not applied. | **0123 (RETRACTED post-HRMS gap channel; numeric strict = keep original 0123)** |
| 0124 | — (no local file) | — | — | N | — | — | — | Numerical gap is OK; runners skip missing. GAP 0124 = NEVER invent placeholder file. | Gap; do not invent (skip) |
| 0125 | — (no local file) | — | — | N | — | — | — | Gap; do not invent | Gap; do not invent (skip) |
| 0126 | `0126_payment_voucher_create_request_tracking.sql` | `payment_voucher_create_requests` | **UNVERIFIED-LOCAL-ONLY** → assume N (proxy: FK `payment_voucher_create_requests_payment_voucher_id_fkey` visible in relationships of supabase_get_tables payment_vouchers → table MAY exist remotely but columns not verified). Treat as **Local-Only Not Re-run-able until explicit verify.** | Y | No (CREATE TABLE IF NOT EXISTS + CREATE INDEX IF NOT EXISTS) | 0122 (0123 optional) | **N (CORRECTIVE G4 CANCELLED out-of-order / post-gap proposals)** | Numeric strict only: runner applies 0122 → 0123 → skips 0124/0125 → 0126 → then hits 0127 HR bundle. CORRECTIVE G6 HOLD: not applied. G10 HR 6-gate 2/6 green → block at 0127 apply. | **0126 (RETRACTED 0142 post-HRMS channel; numeric strict)** |
| 0127 | `0127_hrms_business_events_outbox.sql` | HRMS Bundle 1/9 | **N** — Proxy-verified: supabase_get_tables returns zero HRMS tables (hrms_organisation / employees / memberships / etc. all absent). Confirmed NOT applied. | Y | No (CREATE TABLE only). | 0126 (numeric strict; CORRECTIVE G4) + CORRECTIVE G10 6-gate ALL GREEN mandatory (6/6) before remote apply. | N (bundle; apply only with 0127→0135 in numeric strict order) | **CORRECTIVE G9/G10 HOLD BLOCKED:** 24 HR blocker status = OPEN(21) CODE_FIX_PRESENT(1) AUTOMATED_EVIDENCE_PASS(2) DB_INTEGRATION_PASS(0) RESOLVED(0). CORRECTIVE G10: 6-gate 2/6 green only → APPLY_READY = NO → REMOTE APPLY NO. Never interleaved out-of-band. | 0127 (unchanged until HRMS gate) |
| 0128 | `0128_hrms_core_organisation.sql` | HRMS Bundle 2/9 | **N** — HRMS organisation tables absent remotely. | Y | No | 0127 (numeric strict) | N (bundle) | HR gate G10 6/6 ALL GREEN required. | 0128 (numeric strict) |
| 0129 | `0129_hrms_employees_core.sql` | HRMS Bundle 3/9 | **N** — employees tables absent | Y | No | 0127 + 0128 (numeric strict) | N (bundle) | HR gate | 0129 (numeric strict) |
| 0130 | `0130_hrms_sensitive_subtables.sql` | HRMS Bundle 4/9 | **N** | Y | No | 0127–0129 (numeric strict) | N (bundle) | HR gate | 0130 (numeric strict) |
| 0131 | `0131_hrms_reporting_employment_documents.sql` | HRMS Bundle 5/9 | **N** | Y | No | 0127–0130 (numeric strict) | N (bundle) | HR gate | 0131 (numeric strict) |
| 0132 | `0132_hrms_memberships_feature_flags.sql` | HRMS Bundle 6/9 | **N** | Y | No | 0127–0131 (numeric strict) | N (bundle) | HR gate | 0132 (numeric strict) |
| 0133 | `0133_hrms_approval_subsystem.sql` | HRMS Bundle 7/9 | **N** | Y | No | 0127–0132 (numeric strict) | N (bundle) | HR gate | 0133 (numeric strict) |
| 0134 | `0134_hrms_rbac_roles_permissions.sql` | HRMS Bundle 8/9 | **N** | Y | No | 0127–0133 (numeric strict) | N (bundle) | HR gate | 0134 (numeric strict) |
| 0135 | `0135_firm_operating_settings.sql` | HRMS Bundle 9/9 | **N** | Y | No | 0127–0134 (numeric strict) | N (bundle) | HR gate | 0135 (numeric strict) |
| 0136 | `0136_pv_workflow_escalation_status_history.sql` | Accounting PV F1/F2 cols + user_notifications extended cols (`status / ack / esc / resolved / target_scope / dismissible / severity`) on both `payment_vouchers` and `user_notifications` | **N** — PROXY-VERIFIED ABSENT: `supabase_get_tables` shows payment_vouchers = 57 columns. 0136 would introduce `responsible_lawyer_id`, `approving_partner_id`, `quotation_id`, `quotation_claim_warning`, `rejected_*`, `completed_*`, `last_escalation_notified_at`, `escalation_repeat_count`, `escalation_resolved_*` → remote column count (57) matches only 0122 baseline. Therefore 0136 definitely NOT applied. | Y | No (all ALTER TABLE ADD COLUMN IF NOT EXISTS; CREATE TABLE IF NOT EXISTS for pv_status_history if any; pure additive). CORRECTIVE G7: destructive-0 ≠ Migration Safe. UNIQUE/CHECK/FK/RLS/Trigger need actual UP-test, shadow test, data preflight (Stabilisation). | 0135 (numeric strict CORRECTIVE G4). Depends 0122 baseline + 0136 structural cols present. | **N (CORRECTIVE G4 CANCELLED out-of-order apply; no proof runner supports independent apply with correct schema_migrations marking)** | **RETRACTED proposed "out-of-numeric" + "post-HRMS gap channel" (CORRECTIVE G4 invalidates proposals without runner proof).** G6 HOLD: not applied. Also requires T1 pre-apply duplicate-preflight (G8) for 0142 UNIQUE indexes if 0142 applies before 0136 (chain 0123→0126→0127…0135→0136→0137→0138→0139→0140→0141→0142→0143→0144→0145→0146 strict numeric). | **0136 (RETRACTED proposed rename 0136→0144; strict numeric keeps 0136)** |
| 0137 | `0137_case_bottleneck_monitor_and_pv_delay.sql` | Case Monitor F4 tables + RLS + trigger; joins rely on 0136 `responsible_lawyer_id` columns present | **N** (tables absent remotely) | Y | No (CREATE TABLE + trigger). | 0136 (numeric strict G4) | N (strict numeric; G4 cancelled out-of-order) | 0136 dependency + numeric strict runner applies 0127–0135 first (HR gate HOLD blocks before 0136). G6 HOLD: not applied. CORRECTIVE G7 destructive-0 ≠ safe. | **0137 (RETRACTED 0145 rename; strict numeric)** |
| 0138 | `0138_unified_notification_lifecycle.sql` | Notifications F6 lifecycle. CHECK enums on `user_notifications.status/severity`; adds `status_set_at / escalated_reason / resolved_reason / acknowledgement_due_at / resolution_sla_due_at` etc.; 3 extra idxs; explicit RLS + FORCE RLS. | **N** — Not possible for 0138 to exist without 0136 extension cols (it adds to the same 0136-extended schema). 0136 is absent → 0138 absent (transitively). | Y | No — all additive ALTER ADD IF NOT EXISTS / CREATE INDEX IF NOT EXISTS / ALTER TABLE ENABLE RLS (no data rewrite; CHECK added without NOT VALID → not run against heap for verify, so no destructive rewrite). | **0136 must run BEFORE 0138 numeric strict.** 0136 alone creates status/severity/target_scope/dismissible cols. 0138 alone creates CHECK + status_set_at/escalated_reason + extra idxs. Both safe individually; ORDER MUST be 0136 → 0138 (already numeric strict so default OK). | N (strict numeric; G4 cancelled out-of-order proposals) | 0136 + numeric 0127–0135 HOLD blocks. Must run immediately after 0136 (already true in ascending numeric runner). G6 HOLD not applied. G7 destructive-0 ≠ safe (RLS change may break runtime operations). | **0138 (RETRACTED 0146 rename; strict numeric)** |
| 0139 | `0139_file_custody_release_receipt.sql` | File Custody F7 tables (file_custody_items + movements) + RLS | **N** — tables absent remotely (supabase_get_tables: custody tables / case_bottleneck_snapshots / monitor logs all absent) | Y | No — CREATE TABLE IF NOT EXISTS; no ALTER of existing rows | 0138 (numeric strict G4; not strictly required for schema but runtime pushCustodyNotification writes to 0138 schema; safest after 0136–0138). | N (strict numeric G4) | G6 HOLD not applied. G7: Static destructive=0 → not Migration Safe. FK orphan / RLS integration / trigger behaviour → need shadow UP test + data preflight (Stabilisation). | **0139 (RETRACTED 0147 rename; strict numeric)** |
| 0140 | `0140_notification_resolution_mode_and_dedupe.sql` | Notifications F6 corrective cols: `resolution_mode`, `rule_code`, `correlation_id`, event/recipient split (`entity_type`, `entity_id`), cadence (`last_notified_at`, `next_notify_at`, `delivery_count`), unique-active identity index + 6 extra idxs. | **N** — 0136 absent → 0138 absent → 0140 transitively cannot have been applied (it ADDs ALTERs to user_notifications SCHEMA that even at 0136-level still does not include resolution_mode etc.; and 0140 cols not present in 0122 baseline remote). | Y (LOCAL-NEW, created in T corrective) | No — ALTER TABLE ADD IF NOT EXISTS + CREATE INDEX IF NOT EXISTS, all additive. | 0138 must exist first (numeric strict G4). | N (strict numeric G4) | 0136 → 0138 bundle in numeric strict; overall G6 HOLD not applied until HR 6-gate green. | **0140 (RETRACTED 0148 rename; strict numeric)** |
| 0141 | `0141_file_custody_correctives.sql` | File Custody T-level correctives. | **UNVERIFIED-LOCAL-ONLY** → assume N (remote has no custody tables at all → 0141 correctives naturally not applied). | Y | No | 0139 (numeric strict G4) | N (strict numeric G4) | 0139 dependency. G6 HOLD. | **0141 (RETRACTED 0149 rename; strict numeric)** |
| 0142 | `0142_accounting_permissions_unique_constraints.sql` | T13 P1: UNIQUE indexes `uq_invoices_firm_invoice_no`, `uq_receipts_firm_receipt_no`, `uq_permissions_role_module_action`. | **UNVERIFIED-LOCAL-ONLY** → assume N (index presence not visible via supabase_get_tables; the preflight at `scripts/src/remote-migration-and-duplicate-preflight.mjs` was blocked from issuing pg_indexes SELECT). See `docs/accounting-duplicate-preflight.md` for "UNABLE TO VERIFY VIA DIRECT SQL" status. MUST run duplicate-preflight SELECTs before 0142 is applied to remote. | Y | No (CREATE UNIQUE INDEX IF NOT EXISTS — no heap rewrite for new index creation; but blocks on duplicates). **CORRECTIVE G7 WARNING: UNIQUE index creation may fail on existing duplicates (SQLSTATE 23505). Static destructive=0 does NOT guarantee runtime safe.** | 0122 (creates invoices/receipts/permissions tables). Also requires ZERO duplicates on the three key-tuples (invoice number / receipt number / permissions role+module+action). Numeric strict G4: runner applies 0141 → 0142. | N (strict numeric G4) | **CORRECTIVE G8 STOP CONDITION:** If duplicates exist → STOP. DO NOT auto DELETE / merge / pick newest / pick oldest. Build exact remediation report, then ask User. This is allowed stop condition. G6 HOLD: not applied remotely. | 0142 (locked filename; applies before any 0143 sequence seeding so that sequence-driven numbers cannot collide with pre-existing duplicates) |
| 0143 | `0143_firm_scoped_number_sequences.sql` | NEW. Creates `firm_number_sequences (firm_id, seq_name) PK`, FK→firms(id), RLS enforced+policy, backfills per firm via regex-parsed MAX or COUNT(*)+1 fallback. | **N** (brand new local file). | Y (just authored). | No — CREATE TABLE IF NOT EXISTS + INSERT ON CONFLICT DO NOTHING. Zero UPDATEs / zero existing row rewrite. | 0122 (invoices/receipts/payment_vouchers + firms tables exist). Also safest: apply AFTER 0142 (so UNIQUE index is live BEFORE any generator can consume from `next_value`; i.e. 0142 guards + 0143 generates = no-race combined). Numeric strict G4: runner applies 0142 → 0143 → next migrations in order. | N (strict numeric G4: out-of-order apply CANCELLED, no runner proof) | G6 HOLD not applied remotely. None standalone; ordering preference already satisfied by numeric strict 0142 → 0143. | 0143 (strict numeric) |
| 0144 | `0144_case_reference_history.sql` | Case Reference History immutable append-only table (F9 Corrective) | **N** — case_reference_history table not in proxy results. | Y | No (CREATE TABLE IF NOT EXISTS + RLS; append-only immutable trigger raises on UPDATE/DELETE) | 0143 (numeric strict G4; logically follows firm numbering cases chain) | N (strict numeric G4) | G6 HOLD not applied remotely. CORRECTIVE G7: trigger behaviour changes runtime; needs shadow UP test. | 0144 (strict numeric; already final since P2 collision resolved keeping F9=0144) |
| 0145 | `0145_einvoice_scaffold.sql` | e-Invoice scaffold: invoices.einvoice_* cols + einvoice_submissions table (F15 Corrective) | **N** — absent | Y | No (ALTER ADD IF NOT EXISTS; CREATE TABLE IF NOT EXISTS; UNIQUE on idempotency_key). **CORRECTIVE G7: UNIQUE idempotency_key needs no pre-existing duplicates on new table (safe), but invoice classification RLS/foreign key orphan checks still required.** | 0144 (numeric strict G4; P2 collision resolved F15=0145) | N (strict numeric G4) | CORRECTIVE G14: PARTIAL/EINVOICE_SCAFFOLD_COMPLETE only. CORRECTIVE G15 Production 503 EINVOICE_SANDBOX guard preserved. G6 HOLD not applied remotely. CORRECTIVE G16: If LHDN/MyInvois real API conflicts with User model → LEGAL_TAX_DECISION_REQUIRED + STOP single item only. | 0145 (strict numeric; P2 collision resolved final) |
| 0146 | `0146_case_monitor_kind_widen_approval_waiting.sql` | Case Monitor Kind Widening: approval_waiting enum + bottleneck escalation config CHECK | **N** — absent; but note: 4 monitor/custody tables WERE created by OOB VSR_ADDENDUM_P0 (not 0146 numeric) so monitor_kind widening CHECK has NOT been applied via canonical chain. | Y | No (safe widening: DO $$ DROP CONSTRAINT IF EXISTS → ADD new CHECK). **CORRECTIVE G7 WARNING: Static destructive=0 (widening only) but CHECK widening/narrowing may fail if existing data violates new constraint (unlikely here, widening). Still need shadow UP test.** | 0145 (numeric strict G4) | N (strict numeric G4) | CORRECTIVE G17: monitor_kind CHECK enum widened to 6 values (NOT 9 detectors; severity/ownership/auto-resolve 3 fields SEPARATE; retracted old label "9 detector extension"). G6 HOLD not applied remotely. | 0146 (strict numeric) |
| **0147** | `0147_supporting_documents_unified.sql` | Supporting Documents Unified: creates `supporting_documents (24 cols)` — scope=case/project FK; 9 indexes; 5 FKs → firms/cases/projects/developers/users + explicit conname IF NOT EXISTS DO $$ guard; RLS + 4 policies (firm-select/insert/update/delete); GRANT app_user; updated_at trigger; table+column COMMENTs. | **Y — PROXY-VERIFIED APPLIED PRODUCTION.** 2026-08-11 supabase_get_tables direct match: supporting_documents exists, 24 cols exact match including scope CHECK enum ['case','project'], status CHECK enum ['active','superseded','archived'], SERIAL id PK, 5 FKs present with naming pattern `fk_supporting_docs_*`, default values match, comments match. **NO SCHEMA_DRIFT detected.** Objects present exactly match canonical SQL contract. | Y | No — pure CREATE TABLE IF NOT EXISTS + CREATE INDEX IF NOT EXISTS + ALTER ADD FK IF NOT EXISTS DO $$ guarded + CREATE POLICY IF NOT EXISTS + DO trigger install guarded. Zero UPDATE or DELETE of existing data. 0 rows rewritten. | 0146 (numeric strict G4). Practically needs only base tables firms/cases/projects/developers/users (pre-0100). | N (strict numeric G4). Note: actual prod apply was via P0 direct apply; canonical numeric runner has NOT reached 0147 yet (HR gate 0127 blocks runner at 0127 in full strict chain); but the OBJECTS ARE PRESENT in prod because of out-of-band direct apply. Objects = match. SCHEMA_DRIFT = NONE. | **STATUS = OBJECTS PRESENT IN PROD (direct P0 apply). Canonical file is the SINGLE SOURCE OF TRUTH for object contract. DO NOT renumber. DO NOT re-create objects; if runner reaches 0147 in future numeric chain, all IF NOT EXISTS guards make re-apply idempotent at DDL level (but schema_migrations history will still mark it as a single row — that is correct, it IS the canonical migration).** Checksum = file SHA256 (see §4c). | **0147 (strict numeric; LOCKED. Production objects already match exactly. Source restored from _tmp_excluded_from_typecheck workaround dir during this P0 corrective pass to canonical path.)** |
| **0148** | `0148_platform_entitlements_billing_ledger_usage.sql` | Platform Entitlements & Billing Ledger schema: CHECK enum firms_subscription_status + firm_invoices.status extended DRAFT→OVERDUE; creates 6 tables: `platform_features`, `plan_entitlements` (RLS founder-visible only), `firm_entitlement_overrides` (RLS firm), `subscription_history` (RLS firm+founder), `billing_ledger` (RLS + immutable append-only trigger raises on UPDATE/DELETE), `usage_counters` (RLS firm). GRANTs. | **N — PROXY-VERIFIED ABSENT.** supabase_get_tables 2026-08-11: all 6 tables absent. OOB duplicate vsr_p3_platform_*.sql existed locally ONLY (never applied; deleted in P0 corrective). | Y | No — all CREATE TABLE IF NOT EXISTS / ALTER TABLE ADD IF NOT EXISTS / GRANT / CREATE OR REPLACE FUNCTION + DO trigger install guarded. Zero rows rewritten; no destructive DDL. | 0147 (numeric strict G4). Needs firms/ firm_invoices tables (base ≈0050/0083 ranges). | N (strict numeric G4) | **G6 HOLD: NOT applied remotely.** Canonical 0148 is the SINGLE SOURCE OF TRUTH for global entitlements schema. OOB mirror vsr_p3_platform_entitlements_billing_ledger_usage.sql was a local duplicate; DELETED 2026-08-11 as part of this P0 register update (never applied, never will be). | **0148 (strict numeric; locked filename). Duplicate mirror OOB entry retired. Not applied prod.** |
| **0149** | `0149_seed_platform_features_and_plan_entitlements.sql` | Seed Entitlements Part 1: seeds 27 feature_keys into `platform_features ON CONFLICT DO NOTHING`; DO $$ anonymous block populates plan_entitlements rows per plan name lookup for Starter / Pro tiers with boolean deny-list module flags + integer counters. ON CONFLICT DO NOTHING everywhere. | **N — PROXY-VERIFIED ABSENT.** platform_features table absent (from 0148) → seed transitively not applied. OOB mirror vsr_p3a_seed_*.sql was local duplicate only. | Y | No — DATA ONLY migration. All INSERT/DO $$ with ON CONFLICT DO NOTHING. Zero UPDATE/DELETE. Zero schema change. | 0148 (strict numeric G4; creates platform_features + plan_entitlements tables required by seed). | N (strict numeric G4) | **G6 HOLD: NOT applied remotely.** OOB mirror vsr_p3a_seed_platform_features_plan_entitlements.sql = local duplicate only (never applied; DELETED 2026-08-11 P0 corrective). | **0149 (strict numeric). Seed depends on 0148 schema baseline.** |
| **0150** | `0150_full_feature_registry_reseed.sql` | **Meaning definitively resolved: Global Feature Registry DATA Reseed — Part 2 Expansion (NOT schema change).** Creates plan rows Starter/Pro/Enterprise IF NOT EXISTS; DELETE+reseed existing plan_entitlements rows for Starter/Pro; populates temp table `tmp_pf` with **206 full catalog feature_keys** across 19 modules (Dashboard 7 / Cases 26 / Developers 5 / Projects 8 / Documents 17 / Accounting 32 / eInvoice 12 / Comms 21 / HR 21 / RBAC 10 / Contacts 7 / Notifications 11 / HIMS 13 / eKYC 5 / Reports 11 / Settings 13 / Storage 4 / AI 6 / Audit 3 / Platform 10); UPSERT platform_features ON CONFLICT UPDATE; Starter tier has deny-lists + 5 users / 20 GB / 200 cases / 50 new client limits; Pro 30 users / 100 GB / 1000 OCR pages limits. | **N — PROXY-VERIFIED ABSENT.** (0148 schema absent → 0150 cannot apply). OOB mirror vsr_p3_0150_full_feature_registry_reseed.sql was local duplicate only (never applied; DELETED 2026-08-11 P0 corrective). | Y | **Partially destructive: DELETE FROM plan_entitlements WHERE plan_name IN ('Starter','Pro');** followed by re-INSERT. Purpose = Part 2 catalog expansion from 0149's 27 keys → 206 keys + addition of Enterprise plan row. NOT destructive to runtime case/finance data; but destructive to plan_entitlements rows that existed after 0149 (which is zero in prod because 0148/0149 not applied → net = safe). Schema = untouched. | Depends 0148 (schema tables exist) + 0149 (initial seed exists; DELETE targets 0149 rows before re-seed). | N (strict numeric G4) | **MEANING RESOLVED: 0150 ≠ a new schema placeholder. 0150 ≠ Supporting Documents (that is 0147). 0150 = Global Feature Registry Part 2 DATA Reseed only.** Must never be re-purposed for an unrelated migration. If Part 3 expansion is needed → use 0152+. G6 HOLD: not applied remotely. | **0150 (strict numeric; LOCKED MEANING. Part 2 registry expansion data migration only.)** |
| **0151** | `0151_batch_operations.sql` | Batch Operations Audit: creates `batch_operations` table — BIGINT GENERATED ALWAYS AS IDENTITY PK; firm_id/user_id FK; operation_type TEXT; status CHECK ARRAY IN ['queued','running','completed','partial_failure','failed']; 7 JSONB cols: requested_case_ids/ case_ids_count/ payload/ output/ item_errors/ summary; ip, user_agent; created/started/finished timestamps + updated_at; 4 indexes (firm, firm+status, user, created_at desc); trigger trg_batch_operations_updated_at; RLS + **FORCE RLS**; 3 policies (firm_select/firm_insert/firm_update); GRANT app_user; all table+column COMMENTs. | **Y — PROXY-VERIFIED APPLIED PRODUCTION.** 2026-08-11 supabase_get_tables exact match: batch_operations exists; 17 cols exact match; status CHECK enum has exactly 5 values in ARRAY pattern; BIGINT GENERATED ALWAYS IDENTITY; 4 indexes returned by proxy; RLS enabled; **FORCE RLS = true (proxy confirms)**; trg_batch_operations_updated_at present; 2 FKs present firm_id→firms, user_id→users; default values match; comments match. **NO SCHEMA_DRIFT.** Objects present exactly match canonical SQL contract. | Y | No — pure CREATE TABLE IF NOT EXISTS / CREATE INDEX IF NOT EXISTS / ALTER ADD FK IF NOT EXISTS / CREATE POLICY IF NOT EXISTS / DO trigger install guarded. Zero destructive DDL. | 0150 (numeric strict G4). Needs base tables firms/users (pre-0100). | N (strict numeric G4). Note: actual prod apply was via P0 direct apply (objects present); canonical numeric runner has NOT reached 0151 in full strict chain (HR gate blocks at 0127); but IF NOT EXISTS guards ensure idempotency when runner eventually arrives. | **STATUS = OBJECTS PRESENT IN PROD (direct P0 apply). Canonical file is the SINGLE SOURCE OF TRUTH for object contract. DO NOT renumber. DO NOT recreate; idempotent guards present. NO SCHEMA_DRIFT detected between file and production.** | **0151 (strict numeric; LOCKED. Production objects already match exactly. Canonical migration retained as-is.)** |
| **VSR_P3_DUPLICATE (OOB)** | `vsr_p3_platform_entitlements_billing_ledger_usage.sql` + `vsr_p3a_seed_platform_features_plan_entitlements.sql` + `vsr_p3_0150_full_feature_registry_reseed.sql` (3 files — previously at `supabase/migrations/` — **DELETED 2026-08-11 during P0 corrective**) | Duplicate/Mirror OOB migrations created locally in direct violation of OOB freeze rule. File headers explicitly stated "Mirrors lib/db/migrations/0148/0149/0150" respectively. Exact 1:1 line-for-line copy of canonical migrations for 0148/0149/0150. Never applied remotely. No prod objects from these files. | **N — NEVER APPLIED LOCALLY OR REMOTELY.** Verified: 0148's 6 tables absent in prod (supabase_get_tables). | **N — FILES DELETED 2026-08-11 P0 corrective.** (Historically existed local only; never pushed.) Files restored from prior commits only if audit of exact duplicates required later — canonical files 0148/0149/0150 contain identical content. | N/A (files deleted). | Not applicable (mirrors). | NEVER (OOB frozen). | **ACTION TAKEN = DELETED. Duplicates of canonical migrations 0148/0149/0150 are not required — canonical numeric chain is SINGLE SOURCE OF TRUTH.** Retention was not required because the canonical files contain identical content and are the authoritative migration definitions. Any future lookup of "what did vsr_p3 X do" = read canonical 0148/0149/0150. | **OOB-DUPLICATE-DELETED (Supersedes: 0148 / 0149 / 0150 canonical). Not renumbered into numeric chain because content identical = redundant.** |
| **VSR_DOC_GEN_LOGGING** (out-of-band; NOT part of numeric 0000-0151 canonical chain) | `vsr_p2_doc_gen_logging_additive.sql` (file located at `supabase/migrations/` — NON-canonical OOB staging dir) | VSR Corrective Patch Part 1 out-of-band migration: document_generation_logs (4 new nullable cols: job_id/error_code/error_message/request_id + NON-DESTRUCTIVE CONSTRAINT REPLACEMENT superset CHECK 13 values + junction table doc_gen_log_cases + 2 indexes) + audit_logs (5 new nullable cols: request_id/event_id/correlation_id/ip_address/user_agent + NON-DESTRUCTIVE CONSTRAINT REPLACEMENT widening action CHECK superset + 3 indexes). | **YES (REMOTE_APPLIED_OUT_OF_BAND)** — proxy-verified via supabase_get_tables column matches + prior round `supabase_apply_migration` executed | Y (file exists under OOB dir) | No — officially NON-DESTRUCTIVE per §5: NON-DESTRUCTIVE CONSTRAINT REPLACEMENT + NULLABLE COLUMN ADDITION. 0 rows deleted; 0 rows rewritten; existing action_type values literal subset of new superset CHECK. | Base tables present: depends on document_generation_logs / audit_logs / cases / users / firms tables (base canonical numeric migration establishes these — base likely ≈ 0083 range for doc gen tables, ≈ 00xx initial for audit_logs base; exact TBD via direct-SQL when 5432 reachable). | **NEVER re-apply out-of-order (CORRECTIVE G4 rule).** This entry is RECONCILIATION ONLY. OOB migration channel FROZEN effective this round. All future NEW schema changes go ONE canonical numeric chain ONLY = lib/db/migrations/* ascending + register update. Duplicate apply prohibited. | **STATUS = REMOTE_APPLIED_OUT_OF_BAND.** Direct-SQL verification of exact installed_at/version row UNABLE TO COMPLETE from this runner (5432 firewall; pooler tenant id mismatch). Pending DB-access operator run: `SELECT version, name, installed_at FROM supabase_migrations.schema_migrations ORDER BY installed_at DESC LIMIT 10;` → capture exact version string for this row and fill below reconciliation note field. Re-apply DANGER: do NOT duplicate apply (guards IF NOT EXISTS + DROP IF EXISTS make re-apply technically safe at DDL level but history row pollution is unsafe; runner uses authoritative history so skip once marked). | **(VSR OOB entry — NOT a canonical numeric migration; never renumber into 0147-0151 slot; next canonical numeric = 0152 or higher per LS lib/db/migrations at time of change.)** |
| **VSR_ADDENDUM_P0_RUNTIME_SCHEMA_PARITY** (out-of-band; NOT part of numeric 0000-0151 canonical chain) | `vsr_addendum_p0_runtime_schema_parity.sql` (file at `supabase/migrations/` — NON-canonical OOB staging dir, FROZEN path) | VSR URGENT RUNTIME ERROR ADDENDUM §4/§9 P0 corrective OOB migration. **Exact objects created/modified:** (A CREATE) TABLES: `case_bottleneck_snapshots` (19 cols), `case_monitor_logs` (11 cols), `file_custody_items` (30 cols), `file_custody_movements` (30 cols); (B DO-block ALTER ADD) `user_notifications` adds 30 cols (status/severity/target_scope/target_role_id/dismissible/acknowledged_at/acknowledged_by/escalated_at/resolved_at/resolved_by/auto_resolved_at/status_set_at/escalated_reason/resolved_reason/ip_address/user_agent/acknowledgement_due_at/resolution_sla_due_at/resolution_mode/rule_code/correlation_id/entity_type/entity_id/last_notified_at/next_notify_at/delivery_count/event_resolved_at/event_auto_resolved_at/event_escalated_at/updated_at) + legacy backfill status_set_at; `invoices` adds 9 einvoice_* cols (einvoice_status/einvoice_external_submission_id/einvoice_submitted_at/einvoice_last_checked_at/einvoice_error_code/einvoice_error_message/einvoice_retry_count/einvoice_classification/einvoice_source_invoice_id); (C INDEX IF NOT EXISTS) 9 user_notifications idxs + 3 bottlenecks + 2 monitor_logs + 7 custody_items + 5 custody_movements + 2 einvoice idxs; (D RLS) ENABLE RLS on all 4 new tables (DO block); (E Trigger) `trg_user_notifications_updated_at` (guarded IF moddatetime pg_proc EXISTS, EXCEPTION WHEN OTHERS NULL). | **YES (REMOTE_APPLIED_OUT_OF_BAND)** — `supabase_apply_migration` returned SUCCESS; post-apply `supabase_get_tables` (2026-08-10) confirms 4 new tables exist (case_bottleneck_snapshots, case_monitor_logs, file_custody_items, file_custody_movements), user_notifications column count ≫ 13 baseline, invoices column count ≫ 19 baseline. | Y (file present under OOB dir; DO NOT delete/rename — already applied remotely, file retained for reconciliation/idempotency guard per OOB freeze rule #2) | No — STRICTLY NON-DESTRUCTIVE / ADDITIVE. All operations IF NOT EXISTS: CREATE TABLE IF NOT EXISTS, ALTER TABLE ADD COLUMN IF NOT EXISTS, CREATE INDEX IF NOT EXISTS, ENABLE RLS via DO block with EXCEPTION WHEN OTHERS. ZERO row deletions, ZERO existing DDL destructive replacement beyond widening (invoices einvoice_status defaults DRAFT for NULLS, no update rewrite). moddatetime trigger skipped if function absent (safe). Base tables users/firms/cases/payment_vouchers required by FK REFERENCES clauses exist (≈00xx baseline canonical). | Strictly dependent on base canonical migrations (0000…0122) which created firms/users/cases/payment_vouchers/user_notifications/invoices. No dependency on 0127–0135 HRMS bundle. Intentionally OOB for P0 runtime stability; canonical 0136–0146 migration files remain authoritative source of overlapping objects. Duplicate apply technically safe at DDL (all IF NOT EXISTS) but STRICTLY PROHIBITED to avoid double-marking history rows. | **STATUS = REMOTE_APPLIED_OUT_OF_BAND; OOB channel FROZEN per this register entry.** Any future migration to these object sets MUST go through canonical lib/db/migrations/* numeric chain AFTER reconciliation (see §5 Canonical overlap below). Direct-SQL schema_migrations version row for this file TBD when DB 5432 reachable (currently blocked per §1a). Never delete/rename this source file — retained for exact remote-state match audit. | (VSR OOB entry — NOT a canonical numeric migration; NEVER slot into 0147+ numeric range; file path remains `supabase/migrations/` frozen directory.) |
| **VSR_RECONCILE_PV_IDEMPOTENCY** (out-of-band) | `reconcile_pv_idempotency_and_actions.sql` (file present at `supabase/migrations/`) | PV reconciliation OOB applied idempotency/action corrective. | **YES (REMOTE_APPLIED_OUT_OF_BAND)** — per prior round historical register. | Y (retained under OOB dir per freeze rule #2) | No | Base tables PV / actions exist (0122). | Never (frozen) | **STATUS = REMOTE_APPLIED_OUT_OF_BAND. Retained file not deleted.** | (OOB frozen; do not touch) |


---

## 3. Supabase CLI / Runner Behaviour (Updated CORRECTIVE G4 strict)

| Assumption | Status (CORRECTIVE G4 updated) |
|---|---|
| Runner applies migrations in numeric filename order ascending. | **SINGLE SOURCE OF TRUTH ORDER PER CORRECTIVE G4.** Until formal proof that runner supports independent apply with correct schema_migrations.version history marking (e.g. independent `--only 0136` + history row insert without touching 0127-0135), DO NOT rely on any out-of-order proposal. Assume strict ascending ONLY. |
| Missing numeric gaps (0124 / 0125) → skipped, no block. | **Assumed + EXPLICITLY codified per CORRECTIVE G5:** do NOT invent placeholder files for 0124/0125. Runner skips. |
| Ascending runner hits: 0120 → 0121 → 0122 → 0123 → SKIP 0124 → SKIP 0125 → 0126 → 0127 (HRMS → G10 6-gate not green → HOLD block at 0127 apply) → never reaches 0136–0146 until G10 6/6 green. | **EXPECTED BEHAVIOUR; CORRECTIVE G10: G6 HOLD active until 6/6.** |

### CORRECTIVE G4 Mitigation CANCELLED vs §6 old register:

| Action | Status / CORRECTIVE G4 Verdict |
|---|---|
| Rename 0136 → 0144; 0137 → 0145; 0138 → 0146; 0139 → 0147; 0140 → 0148; 0141 → 0149. "Clear HRMS gap channel" proposal. | **EXPLICITLY CANCELLED (CORRECTIVE G4).** No proof that Supabase migration runner correctly updates schema_migrations history when apply is split across renumbered bands after the fact. P2 collision already resolved: F9=0144 case-reference-history + F15=0145 einvoice scaffold ALREADY occupy 0144/0145 slots. F4 extension monitor_kind widening ALREADY occupies 0146. Renumbering 0136–0141 → 0147–0152 would create NEW numbers beyond active scope 0122–0146, NOT inside existing range as old proposal implied. Therefore renumber INVALID. Keep original 0122–0146 numbers, strict numeric only. |
| Out-of-band `psql -f <file>` apply interleaved: 0123 → 0126 → 0142 → 0143 → 0136 → 0137 → 0138 → 0139 → 0140 → 0141 → 0144 → 0145 → 0146 → separately 0127–0135. | **CANCELLED (CORRECTIVE G4).** Manual out-of-band apply skips 0127–0135 then requires manual marking schema_migrations rows for 0136–0146 first, then later 0127–0135 inserts non-monotonic rows into schema_migrations. No proof runner treats non-monotonic history rows safely for future runs. Too risky for SaaS. Only accept STRICT numeric ascending apply through official runner. |
| Wait for HRMS gate 6/6 green, then apply strictly numeric through 0127–0135 → 0136–0146 entire in one bundle or consecutive runner passes. | **CORRECTIVE G4 ONLY ACCEPTED APPROACH.** Single numeric chain. G10 6-gate ALL 6/6 green prerequisite for 0127. After 0135 completes, runner continues 0136–0146 with no gaps. |

---

## 4. Remote DB schema_migrations.history — Pre-apply Gate (UPDATED CORRECTIVE G4 strict + G6 HOLD + G8 STOP)

`schema_migrations` direct query via **pg.Pool**: BLOCKED (see §1a). Workaround / fallback gate:

### 4a. Indirect proxy-verified matrix (strong column-count evidence — updated 2026-08-11 P0 Corrective)

| Migration Number | What would be present if applied? | Remote proxy-verified (2026-08-11 supabase_get_tables audit) | Conclusion: Objects present? History row known? |
|---|---|---|---|
| 0122 | PV 57 cols (incl. received_by…late_completion_reason); user_notifications FK visible; 0122 settings/actions tables FKs. | PV = 57 columns exactly. FK `payment_voucher_actions_payment_voucher_id_fkey` and `payment_voucher_create_requests_payment_voucher_id_fkey` visible. | **OBJECTS = Y (100%)** — 0122 applied. |
| 0123 | PV idempotency indexes beyond 0122 (uq_payment_vouchers_client_request). | Cannot see index names from proxy; columns 0122 same no change. | **OBJECTS = UNVERIFIED** (index names not returned in proxy tool). Default assume NOT applied (CORRECTIVE G6 HOLD). |
| 0126 | payment_voucher_create_requests table columns beyond FK. | Table may exist (FK visible) but columns not exposed by proxy. | **OBJECTS = UNVERIFIED → default N** (G6 HOLD). |
| 0127–0135 | HRMS tables: hrms_organisation / employees / memberships / sensitive / documents / memberships_flags / approvals / rbac / firm_operating_settings. | supabase_get_tables zero HRMS tables. | **OBJECTS = N** all 9 rows. G10 HR 6-gate APPLY_READY = 2/6 → REMOTE NO. |
| 0136 | PV > 57 cols (→ 58+ due to `responsible_lawyer_id / approving_partner_id / quotation_id / rejected_* / completed_* / escalation_*`) + user_notifications gains 7+ cols beyond 0122 baseline. Note: **VSR_ADDENDUM_P0 OOB has already pushed user_notifications cols independently** (different channel; canonical numeric 0136 still required for proper marking). | PV = **exactly 57 cols**. No extra 0136 cols visible in PV. user_notifications cols: **OOB extended beyond baseline** (VSR_ADDENDUM applied, separate from 0136 numeric chain). | **CANONICAL NUMERIC 0136 = NOT APPLIED (N).** OOB overlap exists — objects partially present via OOB but canonical schema_migrations history row is MISSING. Runner needs to apply 0136 IF NOT EXISTS guards for full numeric chain catch-up (idempotent). |
| 0137 | `case_bottleneck_snapshots`, `case_monitor_logs` tables exist + monitor_kind widening CHECK. | **PRESENT via OOB VSR_ADDENDUM_P0.** case_bottleneck_snapshots (19 cols) + case_monitor_logs (11 cols) both exist via supabase_get_tables proxy 2026-08-11. | **CANONICAL NUMERIC 0137 = NOT APPLIED (N).** Objects present via OOB channel (VSR_ADDENDUM), but canonical history row missing. Runner apply 0137 when reaches it (guards IF NOT EXISTS make it safe). |
| 0138 | `user_notifications.status_set_at / escalated_reason` cols present; CHECK constraints. | **PRESENT via OOB VSR_ADDENDUM_P0.** user_notifications cols: status_set_at/escalated_reason + 28 others visible (30 total OOB-pushed beyond 0122 baseline). | **CANONICAL NUMERIC 0138 = NOT APPLIED (N).** Objects/cols partially present via OOB; canonical history row missing. Runner idempotent apply. |
| 0139 | `file_custody_items`, `file_custody_movements` tables exist. | **PRESENT via OOB VSR_ADDENDUM_P0.** file_custody_items 30 cols + file_custody_movements 30 cols both confirmed present in proxy 2026-08-11. | **CANONICAL NUMERIC 0139 = NOT APPLIED (N).** Objects present via OOB; canonical history row missing. Runner idempotent apply safe. |
| 0140 | user_notifications gains `resolution_mode / rule_code / correlation_id / entity_type / entity_id / last_notified_at / next_notify_at / delivery_count`. | **PRESENT via OOB VSR_ADDENDUM_P0.** All 8 cols confirmed OOB-pushed. | **CANONICAL NUMERIC 0140 = NOT APPLIED (N).** OOB overlap; canonical row missing. |
| 0142 | No column change. Index visible via pg_indexes only. | Cannot verify (blocked from direct SQL access to pg_indexes). | **UNVERIFIED → default N**. CORRECTIVE G8 STOP if any invoice/receipt/permissions duplicate found. |
| 0143 | New table `firm_number_sequences` exists. | Not in proxy result. | **OBJECTS = N**. |
| 0144 | case_reference_history table F9. | Not in proxy result. | **OBJECTS = N**. |
| 0145 | einvoice_submissions F15 scaffold cols einvoice_submissions + invoices.*_einvoice. | **einvoice_* cols on invoices PRESENT via OOB VSR_ADDENDUM_P0 (9 cols confirmed OOB pushed).** BUT einvoice_submissions TABLE = ABSENT in proxy results. | **OBJECTS = PARTIAL (invoices.einvoice_* OOB-present; table einvoice_submissions N).** Canonical numeric 0145 = NOT APPLIED. Runner will create missing table; ALTER ADD IF NOT EXISTS skipped (cols exist). Safe idempotent. |
| 0146 | monitor_kind widened CHECK includes approval_waiting on case_bottleneck. | case_bottleneck_snapshots PRESENT (from VSR_ADDENDUM_P0 OOB) but CHECK enum on monitor_kind = 4 values only, approval_waiting NOT present. | **OBJECTS = N**. CHECK widening NOT applied via any channel. Canonical 0146 still required for full scope. |
| **0147** (Supporting Docs Unified) | `supporting_documents` table exists (24 cols; scope CHECK case/project; 5 FKs; RLS; 9 indexes). | **EXACT MATCH confirmed 2026-08-11.** supporting_documents present. Column count = 24 exactly. Scope CHECK = correct enum. 5 FKs visible (pattern fk_supporting_docs_*). Default values match. Indexes present (proxy). RLS enabled. **NO SCHEMA_DRIFT.** | **OBJECTS = Y (EXACT).** Applied via P0 direct apply. Canonical file = exact match. DO NOT re-create objects directly; runner apply idempotent when catches up. |
| **0148** (Platform Entitlements Schema) | New tables: platform_features / plan_entitlements / firm_entitlement_overrides / subscription_history / billing_ledger / usage_counters. All 6 tables. CHECK firms_subscription_status + firm_invoices.status extended enum. | **ALL 6 TABLES ABSENT.** supabase_get_tables proxy: none of the 6 tables exist. | **OBJECTS = N (definite).** Canonical numeric 0148 NOT applied. |
| **0149** (Seed Entitlements P1 — 27 feature_keys) | Rows in platform_features (27 keys) + plan_entitlements rows for Starter/Pro. | Tables not present → seed cannot have run. | **OBJECTS = N (transitive).** 0149 depends 0148; 0148 absent. |
| **0150** (Feature Registry DATA Reseed P2 — 206 keys) | Dummy tmp_pf table populated; Enterprise plan row; DELETE+RESEED plan_entitlements for Starter/Pro with 206-keys expanded catalog, module deny-lists + tier limits. | Tables not present → reseed cannot have run. | **OBJECTS = N (transitive).** 0150 depends on both 0148 schema + 0149 seed. MEANING DEFINITIVELY RESOLVED = Part 2 Global Feature Registry DATA Expansion (NOT schema). |
| **0151** (Batch Operations Audit) | `batch_operations` table exists: 17 cols; BIGINT GENERATED ALWAYS IDENTITY; status CHECK ARRAY [queued/running/completed/partial_failure/failed]; 7 JSONB cols; 4 indexes; RLS + FORCE RLS; trg_batch_operations_updated_at trigger present. | **EXACT MATCH confirmed 2026-08-11.** batch_operations present; 17 cols exactly; FORCE RLS = true (proxy confirms yes); all CHECK/DEFAULT/comments match; 4 indexes present; trigger name present; 2 FKs present firm_id+user_id; **NO SCHEMA_DRIFT.** | **OBJECTS = Y (EXACT).** Applied via P0 direct apply. Canonical file = exact match. DO NOT re-create.
| OOB VSR_ADDENDUM_P0 | Objects list: case_bottleneck_snapshots (19), case_monitor_logs (11), file_custody_items (30), file_custody_movements (30), user_notifications +30 cols, invoices +9 einvoice cols, indexes/triggers. | **ALL PRESENT exactly per OOB file scope.** Confirmed proxy 2026-08-10/11. | **OOB-APPLIED = YES; canonical numeric overlap rows for 0136/0137/0138/0139/0140/0145 still pending through runner.**
| OOB VSR_DOC_GEN_LOGGING | doc gen logs 4 cols + audit_logs 5 cols + junction + CHECK superset widen + indexes. | Proxy confirms column counts on both tables match expected post-OOB. Audit logs columns > baseline (13+). | **OOB-APPLIED = YES (REMOTE_APPLIED_OUT_OF_BAND).** |
| OOB VSR_RECONCILE_PV | PV idempotency guard + actions tables | Confirmed via prior P0 (direct apply, pv_actions tables present baseline 0122 + idempotency). | **OOB-APPLIED = YES (REMOTE_APPLIED_OUT_OF_BAND).** |

### 4b. Preconditions required before apply ANY batch (strict numeric G4 chain; G6 HOLD lifted → Stabilisation)

**(CORRECTIVE G4 CANCELLED renumber/HRMS-first-or-last flexible options — only strict numeric chain accepted. Updated for P0 source-of-truth scope: 0147–0151 reconciled.)**

1. ✅ Script `scripts/src/remote-migration-and-duplicate-preflight.mjs` exists.
2. ⏸ **Operator with DB TCP access required** (UNABLE currently: blocked 5432 firewall). Run from an environment that CAN reach the DB: Vercel Function shell / DB-access bastion / Supabase SQL Editor. Current runner: cannot reach 5432, pooler tenant id format mismatch.
3. Once §4b #2 accessible environment confirmed:
   - Query authoritative **`supabase_migrations.schema_migrations`** directly for every version 0122–**0151**. This is the ONLY source of truth (not proxy tables). Output exact Number/Filename/Remote History=YES|NO/Remote Schema verified/Dependency/Apply Ready/Blocked Reason matrix (7 columns per CORRECTIVE G4).
   - Specifically verify whether **0147** or **0151** have canonical rows in schema_migrations (even though objects exist via direct P0 apply). If they do NOT have rows (expected — direct apply bypass), they will be re-applied by the runner when it reaches numeric slot (idempotent, all IF NOT EXISTS guarded — safe). That is acceptable — they ARE the canonical definition.
   - **CORRECTIVE G8 duplicate preflight MUST run BEFORE 0142:** exact invoice_no duplicates / receipt_no duplicates / permissions (role_id,module,action) duplicates. Results:
     - If ALL THREE duplicate counts = 0 → 0142 safe to proceed order.
     - If ANY non-zero duplicates → **STOP (allowed stop condition)**. DO NOT auto DELETE / merge / pick newest / pick oldest. Build EXACT remediation report (firm_id, duplicate values, row counts, example PKs), then ask User.
4. **HRMS 0127 prerequisite (CORRECTIVE G10 6-gate ALL 6/6 GREEN required)** (strict numeric runner hits 0127 immediately after 0126; so G10 blocks entire chain at 0127 unless 6/6):
   - ✅ Gate-3: Dependency check PASS (static only)
   - ✅ Gate-6: HR permission matrix PASS (AUTOMATED_EVIDENCE_PASS)
   - ❌ Gate-1: 24 HR blockers RESOLVED all? (0/24 RESOLVED — OPEN 21 CFIX 1 AEVID 2)
   - ❌ Gate-2: Migration UP shadow test PASS? (not run)
   - ❌ Gate-4: RLS DB integration PASS? (not run; only static 21/21)
   - ❌ Gate-5: Destructive + data preflight PASS? (not run; only destructive-lint 0)
5. After G10 = 6/6 green → strictly numeric apply ascending through 0127→0135→0136→0137→0138→0139→0140→0141→0142→0143→0144→0145→0146→0147→0148→0149→0150→0151. Runner skips 0124/0125 automatically; never invent placeholders.
   - For **0147 + 0151 specifically**, objects are already present in prod via direct P0 apply. All DDL in canonical 0147/0151 uses IF NOT EXISTS / DO $$ guard patterns. Runner apply will be idempotent → creates nothing, marks schema_migrations history row for both. That is the CORRECT outcome — closes "object present but no history row" gap.
   - For **0136/0137/0138/0139/0140/0145 (OOB overlap)**, runner apply will hit IF NOT EXISTS → no data rewrite, but fills canonical history rows for object sets that were only OOB-marked. That is the intended reconcile.

---

## 5. Gaps / Follow-up (UPDATED CORRECTIVE G4 G5 G6 G7 G8)

| Item | Status (CORRECTIVE GATE) |
|---|---|
| Direct query of remote `schema_migrations` rows (authoritative, not proxy). | **BLOCKED (pg.Pool credential/pooler format).** Requires DB-access operator in Stabilisation. Workaround proxy = column-count proxy only, not authoritative. |
| Supabase CLI migration runner numeric ordering + skip missing numeric gap behaviour. | **SINGLE SOURCE OF TRUTH (CORRECTIVE G4):** Strict ascending numeric only; skip missing gap 0124/0125 = assumed runner behaviour. No proof provided → accept ONLY. |
| 0136–0141 renumber → 0144–0149 batch (accounting/file-custody channel proposal). | **EXPLICITLY CANCELLED (CORRECTIVE G4).** Renumber INVALID: (1) 0144/0145/0146 already occupied (F9/F15/F4 P2 collision resolved). (2) No proof runner correctly updates schema_migrations history for split bands. Keep original 0122–0146 numbers, strict numeric chain. |
| Out-of-order / out-of-band psql interleaved apply proposal. | **CANCELLED (CORRECTIVE G4).** Too risky for SaaS multi-tenant. Only official runner, strict ascending. |
| HRMS G10 6-gate green (6/6). | **2/6 CURRENTLY.** Stabilisation next. |
| 0142 UNIQUE index apply-gate duplicate-preflight (invoice number / receipt number / permissions role+module+action). | **UNABLE TO VERIFY VIA DIRECT SQL** → explicit in preflight doc. CORRECTIVE G8 STOP CONDITION: any duplicates → STOP + report + ask User (never auto delete/merge). |
| 0143 firm_number_sequences backfill verified against existing MAX invoice/receipt/voucher numbers per firm. | LOCAL migration file prepared with safe COUNT(*)+1 fallback regex parse. Run-time shadow-test UP assertion required Stabilisation (CORRECTIVE G7: not just static destructive 0). |
| CORRECTIVE G7 Static ≠ Migration Safe (all migrations 0123+). | **Each migration needs (Stabilisation):** remote read-only data preflight + shadow/test schema UP-test + schema assertions + RLS integration + row-count reconciliation. Not just static destructive-lint=0. |

---
