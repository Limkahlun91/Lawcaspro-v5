// Phase 0 scaffolding only.
// Sub-module exports below are populated incrementally as each domain is implemented:
//   employees, attendance, leave, claims, payroll, recruitment, performance,
//   training, assets, onboarding, offboarding, approvals, permissions,
//   services, repositories, events, validators.
// Do NOT "export * from ./<subdir>" blindly — each subdir has no TS entry yet.
export {};
